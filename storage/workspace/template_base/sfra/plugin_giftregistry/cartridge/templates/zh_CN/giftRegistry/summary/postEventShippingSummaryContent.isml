<div class="row">
    <span class="address-tittle-summary">${pdict.giftRegistryModel.postEventShippingAddress.name || ''}</span>
</div>

<div class="row">
    <span class="lname">${pdict.giftRegistryModel.postEventShippingAddress.lastName || ''}</span>
    <span class="fname">${pdict.giftRegistryModel.postEventShippingAddress.firstName || ''}</span>
</div>

<div class="row">
    <span class="state">${pdict.giftRegistryModel.postEventShippingAddress.stateCode || ''}</span>
    <span class="city">${pdict.giftRegistryModel.postEventShippingAddress.city || ''}</span>
</div>

<div class="row">
    <span class="summary-addr-1">${pdict.giftRegistryModel.postEventShippingAddress.address1 || ''}</span>
    <span class="summary-addr-2">${pdict.giftRegistryModel.postEventShippingAddress.address2 || ''}</span>
    <span class="zip">${pdict.giftRegistryModel.postEventShippingAddress.postalCode || ''}</span>
</div>

<div class="row">
    <span class="phone">${pdict.giftRegistryModel.postEventShippingAddress.phone || ''}</span>
</div>